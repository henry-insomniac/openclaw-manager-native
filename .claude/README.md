# Project Claude Context

This directory is the local context pack for `openclaw-manager-native`.

Read all files in this directory before substantial work in this repo.

Contents:

- `UI.md`: product positioning and UI direction
- `skills/INDEX.md`: imported skills and when to use them
- `skills/*`: copied skill docs from `~/.agents/skills` so the repo is self-contained

Use this directory as the project-level source of truth for design and UX work.
